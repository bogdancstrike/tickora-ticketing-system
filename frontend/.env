# ── Frontend environment variables (dev defaults) ───────────────────────────
# This file is gitignored (.env). Each developer should maintain their own
# copy. See .env.example for the full list of available variables.

VITE_API_BASE_URL=/tickora
VITE_API_PROXY_TARGET=http://localhost:5100
VITE_PORT=5173
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=tickora
VITE_KEYCLOAK_CLIENT=tickora-spa
